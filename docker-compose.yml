services:
  db:
    image: ankane/pgvector:latest
    container_name: ai_gtm_db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: ai_gtm
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: